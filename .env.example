# Binance API credentials — never commit the real .env file
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Safety gate: must be explicitly set to "true" to allow live order submission
LIVE_TRADING_ENABLED=false

# Logging
LOG_LEVEL=INFO
